import 'package:flutter/material.dart';

class editInputField extends StatelessWidget {
  const editInputField({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}